# Archived specimen — NOT my brand

These files (`resume.json`, `brand.yml`, `gaps.md`, `media-*.md`) are the **instructor's worked
specimen for Nik Bear Brown** that shipped with Exercise One. They are kept here as a reference
example only — they describe his career, not mine.

My actual Exercise One artifacts live one level up in `brand/`:
`resume.json`, `brand.yml`, `gaps.md` for **Kanishk Singh**.

Archived (not deleted, per the repo's no-delete rule) on 2026-06-22 via `git mv`.
